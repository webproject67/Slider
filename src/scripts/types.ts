export interface StateType {
  flag: boolean;
  from: number;
  fromPercent: number;
  max: number;
  min: number;
  range: string;
  scale: boolean;
  step: number;
  to: number;
  toPercent: number;
  view: string;
}
